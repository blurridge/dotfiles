# Frontend Design Skill

IMMEDIATELY load and invoke the `frontend-design:frontend-design` skill before doing anything else.

Use the Skill tool with `frontend-design:frontend-design` to handle this request. Do not proceed with any frontend work until the skill is loaded and active.

Pass along any context or requirements the user provided after the `/fe` command to the skill.
